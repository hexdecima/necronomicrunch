import { fetchSheet } from "../shared/scripts/api.js"
import { RikishiTable, SubmissionTable, ResultTable } from "./data.js"

(async () => {
  // const data = await fetchSheet.sumo();
  // console.log(data.ref);
  // const results = ResultTable.from(data.results.items);
  // console.log(results);
  // const rikishi = RikishiTable.from(data.rikishi.items);
  // console.log(rikishi)
  // const subs = SubmissionTable.from(data.ref.items);
  // console.log(subs);
})();
